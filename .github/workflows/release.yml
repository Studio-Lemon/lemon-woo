name: Release

on:
    workflow_dispatch:
    release:
        types: [published]

jobs:
    call-release:
        uses: Studio-Lemon/workflows/.github/workflows/plugin-release.yml@main
        with:
            plugin_name: 'lemon-woo'
        secrets: inherit
